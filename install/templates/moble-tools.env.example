# Configuração do MÖBI OS na VM
# Copie para moble-tools.env e ajuste antes de iniciar o serviço.

HOST=0.0.0.0
PORT=4173
NODE_ENV=production
MOBI_COOKIE_SECURE=0

# Primeiro administrador (obrigatório na primeira instalação em produção)
MOBI_BOOTSTRAP_ADMIN_EMAIL=admin@empresa.com
MOBI_BOOTSTRAP_ADMIN_PASSWORD=AltereEstaSenhaSegura123

# Portal: false em produção
PORTAL_DEMO_MODE=false
